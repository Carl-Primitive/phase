[package]
name = "phase-card-schema"
version = "0.1.0"
edition = "2021"
description = "Versioned, engine-independent card definition schema."

[dependencies]
serde = { version = "1", features = ["derive"] }
