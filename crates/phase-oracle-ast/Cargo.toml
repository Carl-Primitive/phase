[package]
name = "phase-oracle-ast"
version = "0.1.0"
edition = "2021"
description = "Engine-shaped card definition AST. Serializes byte-identically to phase-engine's card-data.json, without depending on phase-engine."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
