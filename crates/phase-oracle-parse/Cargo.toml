[package]
name = "phase-oracle-parse"
version = "0.1.0"
edition = "2021"
description = "Grammar over the Oracle token stream -> engine-shaped card definitions."

[dependencies]
phase-oracle-ast = { path = "../phase-oracle-ast" }
phase-oracle-lex = { path = "../phase-oracle-lex" }
nom = "8.0"
serde = "1"
serde_json = "1"

[features]
corpus = []

[[example]]
name = "parity"
required-features = ["corpus"]
